        <a href='@Url.Action("Index", "Cart")'>
            <strong>Cart</strong>
            <span id="cart-status" class="badge">
                @*@ViewData["CartCount"]*@
                {{cartCount}}
            </span>
        </a>
